/* Write a function called printNames that accepts an array of 
strings as a parameter and logs each name. 
Pass an array of your choice to the function and call it.

*/





function printNames (values: string[]) {
values.forEach(emptyArray => console.log(emptyArray))


}

printNames(["Sorren", "Jao"]);