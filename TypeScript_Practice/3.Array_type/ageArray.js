"use strict";
/*
Create an array called ages that holds the numbers 18, 25, 30.
Add a new age 40 to the array.
Log the updated array.

*/
const ages = [18, 25, 30]; // declared as number[], meaning it can only hold number
ages.push(40);
console.log(ages); // [..., 40]
