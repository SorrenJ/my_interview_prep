/*
Create an array called colors that holds the strings 
"red", "green", and "blue". 
Add a new color "yellow" to the array. Log the last color in the array.
*/

const colors: string[] = ['red','green','blue'];

colors.push('yellow');


let lastColor = colors[3];
console.log(lastColor); 